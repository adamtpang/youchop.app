# youchop.app — server env (set these in the Vercel project, not committed)

# youtube-transcript.io API token (Profile → API). The Basic-auth token string as-is.
# Plus plan ($9.99/mo) is required for the channel search endpoint; the transcripts
# endpoint works on any paid tier. https://www.youtube-transcript.io/pricing
YT_TRANSCRIPT_TOKEN=
